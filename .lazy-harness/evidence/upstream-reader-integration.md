# Evidence: isolated upstream + Reader integration

## Scope and identity

User-approved preparation only: candidate `/tmp/lh-reader-sync-async-2T2VQR/candidate-7pwx6sze`, detached upstream `80cf3be485d22a8036723e415f8b4e95f897786f`, common ancestor `58fbcbf5e9d632bf9b0e7a87857ad8ed05f01f7b`. Reader input includes the current tracked delta SHA-256 `e38c613e9246e3ffdd5c174ef21751cbd424fc209167fcc5d4a92c44852477c4` plus 23 owned untracked files (including approved local mutation-disabled `.pi-lens.json`). No obsolete 34-path patch was used. Standalone clone, no managed worktree, staging, commit, push, installation, deployment, paid measurement or global change.

## Grounding and isolation receipts

The dedicated Reader tool is unavailable to this worker. Direct fallback read the complete overview, concrete search/read-debt map, actual Reader result/primary-answer TDD and repair/live evidence, production-integration planning, test strategy, search/read-debt contract and code-organization profile. Exact `modifying_source_file` policy/capability resolution in the candidate returned observe-only code-organization guidance.

The initial overview ran in the Reader input and reported rebuilding a missing generated record-index cache. A subsequent absolute candidate CLI invocation still resolved the original cwd; this was not candidate-bound grounding and is not claimed as such. No source/record restore was attempted. This unintended runtime/cache side effect is disclosed separately from source-byte preservation. Subsequent commands use an explicit subprocess cwd and candidate-bound LAZY_ROOT/LAZY_HOST_ROOT. Snapshot tracked diff matches the parent-provided original Reader hash.

Scratch `/tmp/lh-reader-sync-async-2T2VQR` retains `input-manifest.json`, `input-snapshots/{reader,main}/{status.txt,tracked.diff,staged.diff,contents/}`, all owned untracked Reader contents and hashes, `merge-inputs/`, `merge-manifest.json` and bounded `combined-logs/`. Main external drift must be attributed from these receipts, never reverted. Frozen retry-01 A/B native inputs are read-only; installed readfix equivalence is not assumed.

## Merge dispositions

Tracked files use three-way merging from the exact common base, not whole-file overlay. JSONL additions are exact-row append-only unions after asserting both inputs preserve the complete base prefix; historical multi-event IDs are never collapsed. Reader-only deltas are applied only where upstream equals the common base.

- `index.ts`: combine SDK AgentToolResult/MoveProjectDetails with Reader ExtensionContext; preserve contextually typed before_agent_start and prompt-shape overloads; run the child ledger branch before Parent capture receipt completion; retain independent capture reset and Reader run reset; retain capture validation/non-steering notifications plus headless primary-answer and native-drain boundaries. Shared RESULT parser uses a `.ts` module specifier to support upstream Node native stripping.
- `self-test.py`: clean three-way merge retains both upstream repair coverage and Reader launch/status/ledger/fallback regression coverage.
- Pi package SDD/TDD: both append sections retained; upstream typecheck/cache contracts and Reader integration chronology are distinct, not contradictory intent.
- Package manifest: both Reader agent resource and upstream Node22 dev declaration dependency retained. Root manifest/lock/tsconfig, upstream capture module/hooks, graph/sync fixes and host-document checks remain upstream-owned bytes.
- No automatic migration of the pending 37 legacy graph rows. Guided migration remains a separately approved follow-up.

## Validation status

Final standard PASSED after exact-candidate resumption: 294.212s (command294.311s), fresh full regression (`evidenceReused=false`), fast/static46 files passed in0.433s and full self-test90 checks passed in293.667s. Receipt/output: `combined-logs/final-standard.{receipt.json,stdout,stderr}`. Node executable path/version/missing cache-negative checks, graph/sync integrity, host-document scope, Reader guard/status/fallback and Pi contracts all passed. The standard's 300s budget had only about5.8s margin; no timing or fixture-budget repair is included. Successful earlier focused checks were reused at resumption, not rerun. Initial dependency admission paused because original-main dependencies were incomplete and global versions did not match upstream's pinned contracts. Supervisor supplied `/tmp/lazy-harness-init-scope-e1MDLc9i/repo/{node_modules,packages/lazy-harness-pi/node_modules}` for read-only copy into this candidate. `dependency-receipt.json` confirms equal source/candidate lock SHA-256 `1faef350cd6ff99d325414f1f6ea27ad034a3425915c5c7d57683cbba7ac9dce`, TypeScript6.0.3, Node types26.5.0 and workspace22.19.19, Bun types1.3.14, MCP1.30.0 and Pi0.85.1, with zero missing/escaping copied symlinks. No install, downgrade, shim or skipped check. Supervisor also explicitly confirmed adapting the obsolete capture STOP test to actual non-steering typed capture delivery while retaining placement stderr and full-answer checks.

First fast checkpoint failed because copied node_modules was unignored and treated as changed source (19,303 files); all failures were vendored JSONC/binaries. Supervisor approved only the two candidate-local `.git/info/exclude` dependency directories. `dependency-exclusion-receipt.json` verifies the standalone git path and 19,258 dependency-only removals with no owned source/test/record removed. No standalone fast rerun; final standard must run its fast gate.

First combined focused batch: Reader160/164 passed, with all four text CLI cases failing empty stdout despite complete joins/answers; Pi typecheck passed; upstream capture24/25 passed, with the safety-followup fixture accidentally running in default headless mode. Exact failed logs remain under `combined-logs/focused-*`. The SDK's `print-mode.js` outputs text only when the final conversation message is assistant; upstream non-steering custom capture messages therefore hide stdout. Supervisor approved a narrow host reconciliation: typed print/JSON capture uses plain runtime appendEntry plus relevant stderr, while TUI/RPC retains non-steering custom messages. Exact SDK `sessionEntryToContextMessages` excludes plain custom entries. Missing/error hooks retain explicit unverified root/epoch metadata. The SDK capture fixture now explicitly identifies its original interactive assertions as TUI and adds separate print/JSON cases; no original safety assertion was weakened.

Authorized affected-batch rerun PASSED: Reader164/164,761 assertions,118.942s, including all eight real native CLI cases; Pi declared typecheck21.264s; actual Node SDK capture27/27,111.635s. `combined-logs/reconciled-*` preserves exact commands/output; no paid model calls. Each native CLI case retains its raw stdout/stderr/session/ledger traces under the printed `/tmp/reader-native-launcher-*` directory. Prior 164/721/eight-case proof remains historical, not reused. Current declared Pi target emits zero diagnostics, but that is not a universal IDE diagnostic-clean claim: the pre-existing previewContent unknown advisory was not edited and remains disclosed. The live canary's semantic quality was 1/2 per arm; no speed/quality superiority follows from this source integration.

## Discovery capture and layer completeness

This capsule is the single evidence owner for this integration; `.lazy-harness/tests/reader-result-primary-answer.md` owns combined regression scenarios. The implementation maps remain source-confirmed; generated indexes are navigation only.

| Layer | Judgment |
|---|---|
| SDD | Independent transport reconciliation recorded in search/read-debt: headless capture persists full typed runtime entries, not conversation messages; pending/unverified stderr and interactive non-steering metadata retained. |
| BDD | Independent headless clarification recorded in LLM-owned retrieval: even a non-steering custom message must not hide primary stdout; actual native continuation and capture visibility remain intact. |
| SSOT | No independent ownership/config policy delta: exact upstream dependencies and disposable mutation controls retained; no installed/global mutation. |
| DDD | No independent domain rule or vocabulary delta. |

## Timeout and exact-candidate resumption

Run `81f7a0e3-fbd0-4b02-be27-2f869d9167ce` reached its 30-minute overall timeout after all three reconciled focused commands completed successfully. The attempted final-standard tool call was aborted; no final-standard command receipt/output was persisted. Parent confirmed no matching active test processes and preserved `candidate-r3-timeout.{partial.diff,status}` with tracked diff SHA-256 `24da332d9f32513fed3ad92102d24b274c16b4ca1d088d803df41473665b8255`. Resumption reuses the exact candidate and successful focused receipts, without repeating integration, installing dependencies or rerunning green targeted checks. Resumed final standard completed successfully; preservation/patch/handoff closure follows without source/test changes. The matching dependency owner's upstream-only 264.758s validation is historical, not combined proof; the existing copy includes `.bun` and workspace declarations, not hardlinks to the owner checkout.

## Preservation closure

`preservation-receipt.json` verifies all637 inventoried Reader tracked/owned-untracked files unchanged, with the exact original diff/status hashes and no missing files. Of624 original-main inventoried files, only its validation ledger changed: one append-only response-completed event for unrelated `.lazy-harness/planning/v2-research-test-map.md`, retained in `main-validation-drift.json`. This is concurrent lifecycle drift, not a worker source edit; it was not reverted. No blanket all-input-byte immutability claim.

All635 frozen A/B native source files match their initial hashes/modes (excluding `.git`, dependency targets and bytecode). The dependency after-copy comparison covers18,561 regular files and697 symlinks: zero byte differences against the supplied owner snapshot, zero shared source inodes and zero missing/escaping links. This is current byte equivalence, not prior whole-tree authenticity attestation. Of41 upstream changed files,34 remain byte-identical; seven expected overlap paths are reconciled, with no unexpected non-overlap changes. Candidate index has no staged files. Initial original-cwd map/cache side effects remain disclosed above.

This evidence-only closure follows final standard; source, tests and contracts are unchanged after the passing standard and no duplicate validation is run for capsule reporting alone.

## Review boundary

Independent review remains required. This candidate does not authorize main integration, distribution of a native patch, activation, a paid benchmark, search-interface redesign or legacy graph migration.
