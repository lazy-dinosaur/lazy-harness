# Evidence: Reader RESULT and primary-answer core repair

## Scope

User-approved two-core repair only in `/tmp/lazy-harness-reader-runtime-v1`, branch `work/reader-runtime-v1`. Shared strict RESULT parser, synchronous host receipt adaptation, and typed headless print/JSON advisory separation; default async, native code, search lanes and budgets unchanged. Main, installed packages and frozen measurements are not mutation targets. No paid calls, historical rescoring or speed claim.

## Environment

Baseline dirty status, binary diff, staged diff and 682 file hashes: `/tmp/lh-reader-sync-async-2T2VQR/core-repair/before-*`. Exact partial snapshot after the ambient formatter incident: `partial-after-autofix*` in that directory.

Actual installed Pi SDK `docs/extensions.md` defines `ExtensionContext.mode` as tui/rpc/json/print and hasUI false for print/JSON, true for TUI/RPC. Output routing uses typed mode, not user prose or hasUI alone. Existing offline native package copies remain read-only test inputs.

## Commands

- Governing discovery: `lazy map --overview --complete --format=md`, concrete search-read-debt node, actual SDD/BDD/test-strategy/code-organization records.
- Exact source-work policy/capability resolve: `lazy policy resolve --applies-to=modifying_source_file --format=md` and `lazy capability resolve --intent modifying_source_file --format=md`; observe-only code-organization guidance. Initial unsupported policy `--intent` invocation rejected; corrected to documented applies-to syntax.
- Validation receipts and stdout/stderr are retained under the scratch core-repair directory; canonical records do not duplicate full logs.

## Results

- Fast checkpoint PASS: 101 checks, 0.313s (`check-receipt.json`).
- Focused PASS: 164 tests, 721 assertions, 60.067s. All eight actual native CLI combinations (sync/async × JSON/text × production capture/placement) retained every primary-answer claim, exposed the expected advisory on stderr and produced no replacement turn. Async completed after genuine delayed native delivery (`focused-receipt.json`, `focused.stdout`, `focused.stderr`; preserved `/tmp/reader-native-launcher-*` case directories listed in stdout).
- Initial standard FAILED after 69.71s; human output omitted step failure details. Supervisor approved an exact failed-step diagnostic reproduction, retained under `standard-failed-step*`. Two patch-owned integration misses were identified: a legacy static assertion still required the old print-only literal, and the new TDD digest lacked Aliases. Corrected only those tests/record fields; no behavior/source changes or reruns of already-green native/focused tests.
- Final corrected standard PASS: 71.948s, fresh full self-test 88 checks (71.605s), fast-static step passed, evidenceReused=false. Machine receipt `standard-final.json`, execution receipt `standard-final-receipt.json`; prior failures remain intact.
- Existing standard historical guards passed, including Pi package, Reader ledger/status/fallback, steer/root isolation and TUI/RPC continuation. The frozen external historical suites were not modified or rescored; the new CLI fixture reuses their native-launcher/network-guard structure with current host source and actual production hooks.
- Eight baseline files reconstructed from HEAD plus the preserved pre-existing diff (and the planning prefix) match initial hashes exactly; `worker-tracked-and-planning.diff` isolates this worker's changes. No initial file disappeared; only intended record/source edits and six existing pi-lens runtime logs changed among baseline paths. `baseline-diagnostic.json` proves previewContent is unchanged from HEAD and untouched by the pre-existing diff.
- Independent reviewer `963dc495-b832-4faf-a796-05845d7da18f` returned READY/no findings for this scoped patch. Parent read the complete structured review and current result parser; accepted the two-core repair checkpoint. Review inspected actual source/tests, native case traces and retained validation/preservation receipts, without rerunning tests or recomputing hashes. Structured artifact: `/tmp/pi-subagents-uid-1000/async-subagent-runs/963dc495-b832-4faf-a796-05845d7da18f/structured-output/pi-subagent-structured-e7HYBX/output.json`. No staged files, commits, pushes, installs, deployments or paid calls reported.
- Parent cache-only diagnostics had no dispatched files for these worktree paths; that is not a clean-diagnostics result. The unchanged pre-existing advisory below remains explicitly retained. Search-interface work remains deferred and post-repair live performance/quality remains unmeasured. Discovery capture: this capsule closes the approved repair/review unit; no additional production semantic delta beyond the linked SDD/BDD/TDD updates.

Diagnostics: pi-lens reported the pre-existing `previewContent(content: unknown): unknown` return-type advisory at index.ts line410 after the scoped edit. Supervisor explicitly approved retaining that unchanged unrelated diagnostic; no clean-diagnostics claim or unrelated cleanup.

Ambient autofix incident: pi-lens reported markdownlint:1 autofix on the owned planning append (extra blank line), with no other reported source/record mutation. Six pre-existing `.pi-lens-probe-home` runtime logs also changed through normal tool execution; they are not reverted. Exact partial diff/status/hash inventory retained. Work paused and supervisor obtained explicit user approval for ONLY the worktree-local `.pi-lens.json` controls: format.enabled=false, autofix.enabled=false, actionableWarnings.autoFix.enabled=false. No diagnostic disabling or global/main/installed/sibling config change.

The installed resolver confirmed all three controls from source=project; local-controls-resolution.json records no-autoformat=true, no-autofix=true, actionable-warning-autofix=false. The same-session intentionally double-blank-line probe survived postprocessing byte-for-byte (SHA-256 `3131494f99b14bf288807226756c60383338157530e3210115b3500d578da5c1`); local-controls-probe.json records no ancestor mutation flags. This is bounded evidence of current local controls, not universal tool-side-effect immunity.

## Interpretation

Complete requires actual delivered content plus all trusted ledger/hash/epoch/model/task/read-budget checks. Valid incomplete/conflict always remains bounded fallback and never cache. Headless advisory metadata remains visible on stderr without scheduling a replacement assistant answer; required native completion/drain, user steers and action blocks are untouched. TUI/RPC remains unchanged. Offline scripted-provider tests cannot establish fair A/B speed, semantic retrieval quality or deployment readiness.

## Reproduce

Use installed local dependencies without installing anything. Supply `LAZY_READER_NATIVE_SYNC` and `LAZY_READER_NATIVE_ASYNC` as explicit read-only native package paths. Run `bun test packages/lazy-harness-pi/tests/reader-result.test.ts packages/lazy-harness-pi/tests/reader-primary-answer-cli.test.ts`. CLI fixtures use isolated settings, no credentials, deterministic provider, TCP/fetch denial, real native delivery and actual production response.completed hook/helper bytes. Historical suites are retained unchanged. Follow `.lazy-harness/tests/test-strategy.xml`: coherent batch, one fast checkpoint, one focused validation batch, final standard.

## Related records

- `.lazy-harness/tests/reader-result-primary-answer.md` — primary TDD and SDD/BDD/SSOT/DDD matrix.
- `.lazy-harness/spec/platform/search-read-debt-contract.md` and `pi-agent-package.md` — independent component/output delta.
- `.lazy-harness/behavior/llm-owned-record-retrieval.md` — primary-answer behavior.
- `.lazy-harness/planning/reader-production-integration-validation.md` — owned implementation checkpoint; this capsule owns subsequent validation/control receipts.

## Retention / privacy

One durable capsule for this work unit. Scratch retains detailed diffs, hashes, runtime traces and offline fixture logs for acceptance review; no real credentials or raw paid transcripts are copied into canonical records. Existing historical investigations are cited, not rewritten. Local control/probe files are experimental worktree state, not a new framework policy.
