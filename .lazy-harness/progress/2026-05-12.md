# 2026-05-12 — 5c-2 + ADR 0019/0020 정리

## 07:00~ — 어제 마지막 작업 정리

### gorilla session 의 5c-2 tuning 완료

| 작업 | Commit |
|---|---|
| 5c-2 SDD detector 1차 구현 | `48369359 feat(harness): add SDD detector` |
| 5c-2 tuning (noise filter) | `d68adf66 Fix(harness): tune SDD cross-ref noise filtering` |

**검증 결과**:
- 8/8 통과 조건 모두 PASS
- 실코드 src/main/trpc/routers 안 724 candidates
- zod-expr noise 0 / short noise 1 (Emr 약어 — ambiguous 케이스)
- 5c-1 regression 0

### 발견된 framework gap: Ambiguous detection

`Emr` (3글자, EMR 약어) 케이스가 simple short filter 로 제외됨 → false negative.

사용자 통찰:
> "이런 잘 모르겠는경우엔 gate 를 통해서 사람에게 물어보고 확정지을 수 있는거잔아 그지??"

→ ADR 0019 작성. AI 가 confidence 못 매기는 경우 force gate ask.

### TDD framework 위치 명문화 약속 이행

ADR 0018 에서 약속만 했던 "TDD = 5d cross-verify gate" 를 ADR 0020 으로 영구 기록.

## 작업 내역

### 1. ADR 0019 — Ambiguous Detection → Force Gate

- `decisions/0019-ambiguous-detection-force-gate.md` (157 lines)
- 핵심: Confidence × Gate 결합 룰 명시
  - high → auto+review
  - medium → recommend
  - low → force
  - ambiguous → force + structured ask
- Emr 케이스 예시
- 잘못된 패턴 (단순 filter) vs 올바른 패턴 (confidence + gate) 명시

### 2. ADR 0020 — TDD Cross-Verify Gate in 5d

- `decisions/0020-tdd-cross-verify-gate-in-5d.md` (163 lines)
- 핵심: TDD 는 5c detector 아닌 5d sub-task
- 5 cross-check:
  1. Test 존재
  2. Test ↔ BDD
  3. Test ↔ DDD
  4. Test ↔ SDD
  5. Test ↔ SSOT
- 통과 수 따라 confidence + gate 결정

### 3. plan 갱신

- 5c-1, 5c-2 에 `status="done"` / `status="done-with-tuning"` 마커 추가
- 5b addedDuringPhase 에 ADR 0018/0019 note 추가

### 4. handoff 갱신

- ADRs 18 → **19** (방금 19 → 20 위해 더 변경 예정)
- Decisions logged 19 → **20**
- Next phase: "5c-2 acronym tuning (ADR 0019) → 5c-3 BDD detector"

### 5. decisions.jsonl + handoff 동시 갱신 예정

(이번 응답 마지막에 ADR 0019/0020 entry 추가 + ADR count 20 까지 끌어올림)

## 현재 phase 진행 상황

```
5a ✅ closed
5b ✅ closed
5c 🟡 진행 중:
  5c-1 ✅ done (DDD detector)
  5c-2 ✅ done-with-tuning (SDD detector + cross-ref + noise filter)
       └ ✅ acronym ambiguous 처리 완료 (EMR/Emr → ADR 0019 force gate)
  5c-3 ✅ done (BDD detector — 자연어 + UI heuristic)
  5c-4 ✅ done (SSOT detector)
  5c-5 ✅ done (Cross-layer consistency map)
  5c-6 ✅ done (lint/typecheck drift — environment vs code drift)
  5c-7 ✅ done (구조화 옵션 ask validator)
  5c-8 🔵 미시작 (E2E 시연 4 layer + cross-ref)
  5c-9 ✅ done (Doctor C17 + framework-owned doctor D01~D06)
5d 🔵 planned (Interview Loop + TDD gate — ADR 0020)
5e 🔵 planned (실전 1회)
```

### 5c-7 Structured ask validator 완료

- `TriggerRunResult.structuredAskValidation` 추가.
- `validateStructuredAsk(candidate)` / `validateStructuredAsks(...)` 공통 validator 추가.
- 검사 항목: 3~5 options, recommended option 존재, option ID 중복/범위, direct input/skip escape, ambiguous force-gate 문구, high-confidence RecA.
- `lazy:test`에서 모든 fixture candidate + cross-layer integrated ask 를 명시 검증.

### 5c-9 Framework-owned doctor + C17 완료

- `.lazy-harness/scripts/doctor.py` 추가: smoke/full profile.
- `bun run lazy:doctor` 추가.
- D01 XML, D02 JSONL, D03 ADR sequence/docs count, D04 README/handoff/phase freshness, D05 branch/hook policy, D06 external dependency invariant.
- `lazy:test`가 doctor smoke 와 C17 negative fixture 를 검증.

### 5c-6 Lint/typecheck drift detector 완료

- `.lazy-harness/triggers/lint-output.ts` 추가.
- `bun run lazy:lint-drift` 추가.
- tsc output 에서 environment issue (`missing-type-definition`, `missing-config`, `missing-module`) 와 code drift (`type-mismatch`, `property-missing`) 분리.
- eslint compact output 을 `eslint-rule` code drift 로 분류.
- `lazy:test`에서 `typecheck-env`, `typecheck-code`, `eslint-code` fixture summary 검증.


## 누적 변화

| | 어제 | 오늘 |
|---|---|---|
| ADR | 18 | **22** (+0019 ambiguous gate, +0020 TDD gate, +0021 branch/extract, +0022 lazy:test ownership) |
| Decisions logged | 19 | **23+** entries |
| 5c-1 | done | done |
| 5c-2~5c-9 | partial | **5c-2/3/4/5/6/7/9 done**, 5c-8 pending |
| Framework v1.4 | 975 lines | 975 lines + framework-owned `lazy:test`/`lazy:doctor` gates |

## Pending (다음 작업)

- 5c-8 E2E demonstration: 실제 medivance change 1회로 DDD/SDD/BDD/SSOT + lint drift + cross-layer map 검증

## 다음 우선순위

1. **5c-8 E2E demonstration** — 4 detector + lint drift + cross-ref map 실제 시연
2. **Refactor `code-change.ts` monolith** — 5c-8 behavior pin 이후

---

## 08:32 — 5c-2 acronym tuning 완료 (ADR 0019 첫 적용 검증)

### gorilla worker 작업

| Commit | 내용 |
|---|---|
| `9da37c86` | Feat: add acronym ambiguous detection (1차) |
| `6264173f` | Fix: tighten acronym candidate detection (룰 정밀화) |

### 사용자 검증

**1차 (9da37c86)**: 6/8 PASS, 2 문제
- DDD 138 → 125 감소
- isAcronymCandidate true 가 긴 이름 (AttachmentInputSchema, actionRouter, create 등) 에도 잘못 붙음

사용자 catch: "isAcronymCandidate 룰 너무 광범위"

**2차 (6264173f)** — 룰 정밀화:
- 3글자 + all-caps (EMR/UI/IPC) → isAcronymCandidate true
- 3글자 + PascalCase 약어 (Emr/Ipc) → isAcronymCandidate true
- 그 외 → isAcronymCandidate false

**최종 검증 8/8 PASS**:
1. ✅ PatientRiskProfile=high (regression)
2. ✅ orderItemSchema=high (regression)
3. ✅ src/main DDD 137 (138 근처 복구) — EMRVisit, EMRBilling 같은 EMR + suffix 복합 type 정상 복구
4. ✅ Emr ambiguous + isAcronymCandidate + 5opts + RecA
5. ✅ EMR (all-caps) ambiguous
6. ✅ By 제외
7. ✅ ubiquitous-language.xml 등록 후 ambiguous 해제 (matched 로 이동)
8. ✅ 실코드 emrRouter missing 0, ambiguous 분류 정확

### 부가 발견

- src/main 안 isAcronymCandidate=true count: **0** — 실제로 진짜 3글자 약어 type 이 없음. 룰 정밀화 정확.
- 옛 138 → 새 137 (1 감소) — 거의 100% 복구
- fixture 안 Emr/EMR 만 ambiguous + ask 5 옵션

### ADR 0019 의 진짜 framework 가치

```
Emr 검출 → confidence: ambiguous (AI 단정 거부)
       → force gate ask 5 옵션 (Principle 17)
       → 사용자 결정 영구 기록 (ubiquitous-language.xml or forbidden-terms.xml)
       → 다음 호출 시 재발 0
```

→ 사용자 catch 패턴이 framework 자동 메커니즘 안으로 흡수 완료.

## 누적 변화 (오늘)

| | 아침 | 지금 |
|---|---|---|
| ADR | 19 | **20** (+0019 ambiguous gate, +0020 TDD gate) |
| 5c-1 | done | done |
| 5c-2 | done-with-tuning | **done** (acronym 완성) |
| Commits | 1 | **4** (9da37c86, 6264173f, etc) |
| ADR 0019 적용 | 명시만 | **첫 실제 적용 + 검증 완료** |

## 다음: 5c-3 BDD detector

- PRIMARY: last_user_message 자연어 분석 (어려운 task)
- SECONDARY: git diff + UI heuristic
- ADR 0018 cross-ref: DDD/SDD reference check
