# 2026-05-10 — Phase 5b 완료 + ADR 0009/0010 hardening

## 시작 상태

- Phase 5a closed (전 세션)
- Framework contract 690 줄, 19 principle sections
- 8 ADR (0001~0008 — 0008 5b 시작 직후 작성)

## 한 일

### Phase 5b — Lifecycle Hooks (메인 작업)

| Criterion | 상태 | 비고 |
|---|---|---|
| 5b-1 post-commit logs | ✅ pass | actions.jsonl 자동 append 검증 |
| 5b-2 AST contract diff | ⏸ deferred → 5b-2a | ADR 0008 |
| 5b-3 pre-push schema | ✅ pass | Unified Result Schema 출력 |
| 5b-4 regression auto | ✅ pass (beta) | post-commit inline detection |
| 5b-5 weekly snapshot | ✅ pass | 548K backup, ISO-week, 8주 retention |
| 5b-6 1주 실측 | ⏳ passive | 시간 의존 |
| 5b-7 R2 fallback | ✅ pass | `.hooks-disabled` lock |

### 발견 — 5a/5b 전체가 dead code 였음

**marker 실험으로 검증**: `.git/hooks/pre-commit` / `post-commit` / `pre-push` 모두 한 번도 호출 안 됨. 원인: husky 가 `core.hooksPath = .husky/_` 로 path 가로챔.

→ ADR 0009 (Husky Integration Policy):
- `.husky/<3 hook>` 작성 (medivance origin 에 commit)
- `.lazy-harness/hooks/pre-commit-guard.sh` 신설
- dead `.git/hooks/<3 hook>` 삭제
- doctor C5 husky-aware, C11 husky integration, C11b dead hook detection
- init Step 2.6b (lazy hook 작성) + 2.7 (husky/git native 자동 감지 wiring)
- Step 5 (옛 wiring) 제거

### Plan Status Hygiene 발각

사용자 직감: "phase 다 기록하고 하나씩 체크하면서 진행하는거 맞지??"

→ 객관적 점검 결과:
- 5b criteria 의 status 안 박음
- emergent ADR 4 개가 phase plan 의 `<addedDuringPhase>` 에 반영 안 됨
- progress/, plans/ 한 번도 안 박힘

→ ADR 0010 (Plan Status Hygiene = Principle #20):
- closed phase 는 모든 criterion 에 status 박기
- emergent ADR 은 `<addedDuringPhase>` 에 append
- daily progress 박기
- doctor C12 가 자동 검증

## 산출물

### ADR (4 신규)
- 0006 Directory Bridge Architecture
- 0007 AGENTS.md Auto-Injection
- 0008 AST Contract Diff Deferred
- 0009 Husky Integration Policy
- 0010 Plan Status Hygiene Principle #20

### 코드
- `.lazy-harness/hooks/`: 4 sh (pre-commit-guard, post-commit, pre-push, weekly-snapshot)
- `.lazy-harness/scripts/contract-diff.ts`: skeleton + interim helper
- `.lazy-harness/manifests/skills.xml`: 11 skills 선언
- `.husky/<3 hook>`: chain entries
- `.jcode/skills/harness-init/scripts/init-lazy-harness.sh`: 825 → 925 lines
- `.jcode/skills/harness-doctor/scripts/doctor.sh`: 425 → 518 lines (C9, C11, C11b, C12 추가)

### 문서
- `planning/phase-5-plan.xml`: 5b status + addedDuringPhase 박음
- `retrospective/cycles/5b-progress.md`: 5b 회고
- `handoff/00-current-state.md`: 갱신
- `decisions/0006~0010`: 5 ADR

### Logs
- `logs/decisions.jsonl`: 4 entries
- `logs/actions.jsonl`: 47 → 79 entries (+32)
- `logs/validations.jsonl`: 169 → 212 entries

## Lesson

1. **AI 는 검증 안 하고 진행함** — `.git/hooks/` 실 호출 검증을 5a/5b 내내 안 함. Principle 0 가 "이걸 잡아주는 게 framework" 인데, framework 자체에 "marker 실험으로 검증해라" 라는 패턴 명시 안 됨 → 다음 ADR 후보
2. **사용자 직감이 framework 약점 catch** — husky integration / plan hygiene 둘 다 사용자가 짚음. ADR 0005 의 cascade 패턴 또 발현
3. **명시 안 된 의무는 누락된다** — Principle #20 도 같은 lesson. ADR 0005 → 0006 → 0010 모두 동일 사유

## 다음 세션 시작점

1. medivance origin 에 `.husky/<3 hook>` commit 진행 (사용자 confirm 대기 중)
2. 5b-6 (1주 실측) 시간 자연 누적 — `ScheduleWakeup` 가능
3. 5c (Figma adapter) 진입 검토
4. (옵션) ADR 0011 — Verification Discipline (marker 실험 의무화)

---

## 16:00~17:15 — Verification Discipline + Audit Cascade + 5c Re-scope

사용자 trigger 4 회로 ADR 0011/0012/0013/0014 추가됨. 9/14 ADR 사용자 catch 패턴.

### 진행

| 시각 | 작업 | 결과 |
|---|---|---|
| 15:48 | 사용자 "전수조사 하고갈까?" → Oracle + Sisyphus 병렬 audit | Oracle 3 critical gap + Sisyphus 6 NEW silent failure |
| 15:50~16:00 | ADR 0012 작성 + audit cascade fix | JSONL 3 invalid lines 복구, init regression fix, leak guard ALL push, doctor C14/C15/C16 추가 |
| 16:00 | 사용자 "ADR 0009 유지 → exclude 빼고 commit" → husky tracked | commit `b95dd3f7` medivance origin push 완료 |
| 15:52 | 사용자 "구현하면서 나타나는 사실을 기반으로 판단" → Code-First Trigger | ADR 0013 + Principle 22/23 추가, 5c 재정의 (Figma → Code-Trigger) |
| 16:04 | AGENTS.md 갱신 (78 → 142 lines) | 23 principle / 16 doctor checks 반영, init.sh template 도 sync |
| 16:08 | 사용자 "버그 먼저 잡고가야하는거 아닌가??" → bug-4 fix | ADR 0014 — validations.jsonl rotation when >1000 lines |
| 16:10 | 사용자 "1000 줄 넘어가도 누적해야하는거 아닌가? 아 validation 별거 아니구나" | framework 21.8 — Audit log 책임 분리 명시 (actions/decisions 영구, validations 단기) |
| 16:15 | 사용자 "지금 진행상황이나 이런거 전부 체크하면서 하고 있는거지??" | Plan/handoff/progress 갱신 누락 catch (Principle 20 위반 재발) |

### Doctor 변화

C1~C12 → **C1~C16** (4 추가):
- C13 Verify Quality (L0~L4 distribution)
- C14 JSONL Validity
- C15 Husky Tracked (ADR 0009)
- C16 Handoff Freshness

### 누적

| | 시작 | 종료 |
|---|---|---|
| ADR | 10 | **14** (+4) |
| Decisions | 5 | **15** (+10, ADR 0001~0009 backfill 포함) |
| Actions | 93 | **125+** |
| Framework | v1.2 740 lines | **v1.4 975 lines** |
| Doctor | 12+1 (10 pass) | **16 (14 pass)** |
| AGENTS.md | 78 lines | **142 lines** |
| Husky tracked | ❌ | ✅ (b95dd3f7 push) |

### 깨달음

1. **AI 자체 verify = self-report**. Oracle + Sisyphus 가 catch 한 9 항목 모두 AI 가 "5b CLOSED" 라고 자체 선언한 후에도 남아있던 것.
2. **사용자 catch 패턴 9/14**. Framework self-correcting 한계 + 사용자 의존도 높음.
3. **Plan/handoff/progress 갱신 누락 패턴 재발**. ADR 0010 의 본질이 다시 tested. 이번 catch 도 사용자.
4. **wash-out 방지 패턴 정착**. 모든 fix 가 live file + init.sh template 양쪽에 동시 적용 의무 (Sisyphus bug-1 lesson).

### Pending → 다음 세션

- 5c-1 ts-morph PoC 시작 (DDD term detector)
- doctor C12 의 blind spot — handoff stale + plan addedDuringPhase 미갱신 catch 확장
