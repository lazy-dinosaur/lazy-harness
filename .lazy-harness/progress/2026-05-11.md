# 2026-05-11 — M11 Lifecycle Hook live test 성공 + 5c re-scope

## 12:00~13:00 — Stage 6 fix + 우리 framework live test

### M11 progression

| Stage | Commit | 내용 |
|---|---|---|
| 1+2 | `e345d632` | decision parsing + reminder inject |
| 3 | `b78e2bf5` | loop guard (3회 deny) |
| 4 | `f382011c` | client.disconnect vs session.stop split |
| 5 | `dc26f1e8` | payload context (last_user_message, recent_tool_calls, turn_count, session_age_seconds) |
| **6** | **`f372c801`** | **continuation user msg inject (Anthropic API 400 fix)** |

### Live E2E 결과 (12:48~12:49 33초)

**Setup**: stale handoff (line 15 `999 lines`, 실제 975), sync-guard 등록 (blocking=true), 새 jcode 창 medivance cwd.

**1. 첫 fire (12:48:37)** — `stop_hook_active: false`
- sync-guard deny: "STOP. handoff... 999 → 975 ... Edit tool 로 일괄 교체"
- M11 Stage 6: 즉시 continuation user message inject

**2. AI 자동 작업 (33초)**
1. `ls` — handoff 디렉토리 위치
2. `agentgrep` — 00-current-state.md 검색
3. `bash` — find 추가 검증
4. **`edit`** — `999 lines` → `975 lines` (replace_all=true) ✅
5. `agentgrep` — 다른 stale 9으로 시작하는 숫자 확인

**3. 두 번째 fire (12:49:10)** — `stop_hook_active: true`
- sync-guard allow (이미 갱신됨)
- 진짜 stop, 사용자에게 결과 보고

### Framework self-correcting 입증

| 측면 | 결과 |
|---|---|
| 사용자 catch 의존 | **0** |
| 사용자 prompt | **1번** ("안녕") |
| AI 자동 tool calls | **5** |
| Total 시간 | **33초** |
| 사용자 개입 | **0** |

→ 9/15 사용자 catch 패턴 → **0/16 (이번 turn 부터)**.

## 13:00~14:00 — 5c re-scope + 문서 정합성 정리

### 사용자 catch: "5c-6 빼자고 했는데??"

ADR 0013 cascade 가 사용자 통찰 부분만 반영. 3 문서 stale:
- `planning/phase-5-plan.xml` 의 5c-6 (external opt-in)
- `decisions/0013-*.md` cascade table 안 `triggers/external/*`
- `trails/01-long-term-roadmap.xml` M3 "Adapter Funnel (Figma 우선)"
- `handoff/00-current-state.md` "5b-2a → 5c (Figma adapter)"

### Fix cascade

| 파일 | 변경 |
|---|---|
| `decisions/0013-*.md` | `external/` 섹션 제거 + 사용자 발화 trigger source 명시 |
| `planning/phase-5-plan.xml` | 5c-6 삭제, 5c-7→5c-6, 5c-8→5c-7, 5c-9→5c-8 |
| `trails/01-long-term-roadmap.xml` | M3 "Adapter Funnel" → "Code-Trigger Adapters (사용자 발화 + 코드 변경)" |
| `handoff/00-current-state.md` | Next phase = "5c-1 ts-morph PoC", Husky tracked YES, ADR 15→17 |
| `decisions/0017-*.md` | 신규 ADR — User Input as Universal Trigger (5c-6 폐기 명문화) |

### README.md 작성 (framework entry point)

`.lazy-harness/README.md` 154 lines — 처음 진입자가 한눈에:
- 한 줄 개요
- 빠른 진입점 (5 파일 link)
- Lifecycle Hook 메커니즘 mermaid
- Phase 진행 상황
- 디렉토리 구조
- 23 Principles 요약
- Doctor / 다음 단계

## 누적 변화

| | 시작 | 종료 |
|---|---|---|
| ADR | 15 | **17** (+0016 lifecycle, +0017 user-input-trigger) |
| Decisions | 16 | **18** entries |
| Doctor | 13 pass + 1 warn | **14 pass / 0 warn / 0 fail** |
| M11 Stages | 1+2 | **1+2+3+4+5+6** (all done) |
| Live test | 미수행 | ✅ **33초 자동 sync 완료** |
| `.lazy-harness/README.md` | 없음 | **154 lines** |

## 깨달음

1. **Framework self-correcting 진짜 작동** — 6시간 패턴이 1 turn 안으로 흡수
2. **사용자 catch 의 본질** — 9/15 → ADR 0010/0011/0015/0016 로 자동화. 16번째 catch (5c-6) 도 ADR 0017 로 흡수.
3. **3 문서 동시 갱신 의무** (Principle 19/20) — plan / trails / handoff 가 한 결정의 cascade 받음
4. **M11 5 stage = framework 의 missing gate** — 이거 없었으면 framework 의 ADR 0010~0015 가 50% 가치

## Pending → 다음 세션

- 5c-1 ts-morph PoC (DDD term detector)
- 5c-7 medivance 실 코드 변경 1회 E2E 시연
- 5c-8 Doctor C17 (external SaaS grep)

---

## 13:00~15:55 — 5c-1 PoC + ADR 0018 (Cross-Layer Cascade)

### 5c-1 ts-morph PoC 완료

다른 세션 (octopus) 에서 작업:
- ts-morph dependency 추가
- `.lazy-harness/triggers/{types.ts, code-change.ts, fixtures/__patient-risk.ts}` 작성
- `hooks/lifecycle/helpers/check-ddd-trigger.sh` 작성 + `on-response-completed.sh` 등록

**검증 8/8 통과**:
| # | 조건 | 결과 |
|---|---|---|
| 1 | PatientRiskProfile 검출 | ✅ |
| 2 | layer = ddd | ✅ |
| 3 | criterionId = 5c-1 | ✅ |
| 4 | confidence ≥ medium | ✅ high |
| 5 | ask 4 options | ✅ |
| 6 | Recommended A | ✅ |
| 7 | fixture 삭제 후 검출 0 | ✅ |
| 8 | ubiquitous-language.xml 등록 후 검출 0 | ✅ (regex pattern bug fix 후) |

**Bug & fix**:
- `readKnownTerms` 의 regex 패턴이 `<entry canonical=...>` 누락 — pattern 통합으로 fix
- 한 commit (`fix: parse canonical attributes`) 적용

**Heuristic tuning**:
- src/main 전체 scan: 138 candidates 검출
- confidence 분포: high 78 / medium 54 / low 6
- False positive ~15-20% (목표 30% 미만 ✅)
- Required samples 6개 (LeaveByTenureItem, RestoreScheduleEntry, SupplyType, VacationTx, CapturedSchedule, ReplacementRestoreData) 모두 검출

### ADR 0018 — Cross-Layer Cascade

사용자 통찰: "ddd 와 sdd 맵이 만들어지는거겠지? 그 스펙과 ddd 를 통해서 bdd 시나리오도 알수있고 이거로 빠진걸 알수도 있고 tdd 도 알수도 있고 아닌가??"

→ 4 detector (DDD/SDD/BDD/SSOT) 가 isolated 아니라 유기적 cross-reference.
- BDD scenario 의 'then 자동완성 list' → SDD endpoint 'autocomplete' 누락 발견
- SDD type → DDD term 미등록 발견

**5c criteria 재정의** (사용자 통찰 반영):
- 5c-2: SDD detector **+ DDD reference check**
- 5c-3: BDD detector **자연어 우선** + DDD/SDD reference check
- **5c-5 신규**: Cross-layer consistency map
- 5c-6/7/8/9: 번호 재할당

TDD 의 위치 명확화:
- 4 detector = input/development phase
- TDD = output/verify phase (개발 완료 후)
- → 5c 안 detector 아닌 5d Interview Loop 안 cross-verify gate (ADR 0019 예정)

### 누적

| | 시작 | 종료 |
|---|---|---|
| ADR | 17 | **18** (+0018 cross-layer cascade) |
| 5c-1 | 미시작 | ✅ 완료 (8/8 + 138 candidates) |
| Triggers infrastructure | 없음 | ts-morph + types.ts + code-change.ts + fixtures + hook helper |
| 5c criteria | 8 (5c-1~5c-8) | **9** (5c-1~5c-9, cross-layer map 신규) |
| BDD trigger 명세 | 코드 heuristic 만 | **자연어 우선** + 코드 heuristic 보조 |

### Pending

- 5c-1 commit (현재 untracked)
- 5c-2 SDD detector 진입 (with DDD reference check)
- ADR 0019 — TDD cross-verify gate in 5d

---

## 2026-05-11T16:06:10+09:00 — Session disconnect

- session_id: session_lobster_1778482728756_82f9465caeb72509
- turn_count: 0
- session_age: 442s (7 min)
- reason: disconnect

---

## 2026-05-11T16:06:12+09:00 — Session disconnect

- session_id: session_dodo_1778482762295_04042d79f376c185
- turn_count: 0
- session_age: 409s (6 min)
- reason: disconnect

---

## 2026-05-11T16:07:04+09:00 — Session disconnect

- session_id: session_octopus_1778472475664_8b69ef4c02dc11db
- turn_count: 6
- session_age: 10749s (179 min)
- reason: disconnect
