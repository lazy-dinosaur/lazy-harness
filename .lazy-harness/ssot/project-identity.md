# Project Identity and Working Context

Status: accepted
Layer: SSOT
Date: 2026-05-14

## User-confirmed statement

This checkout is the standalone `lazy-harness` framework source-of-truth repo.

The primary project goal in this workspace is to build, tune, and validate the lazy-harness framework itself, including its record-first behavior, lifecycle hooks, prompts, rule grammar, and host-sync/integration assets.

When the user shows screenshots, transcripts, or short ambiguous references during this work, interpret them first as evidence about lazy-harness/Jcode-agent behavior and harness tuning, not as a request to fix an unrelated product UI, unless the user explicitly redirects the task.

## Scope

- Current host: `/home/lazydino/dev/lazy-harness`
- Framework product: lazy-harness
- Local Jcode harness: `.jcode/` is private/local workflow wiring for this checkout.
- Host installs such as `/home/lazydino/dev/medivance/.lazy-harness` are downstream installed copies, not the source of truth.

## Allowed work in this host

- Update lazy-harness framework records, prompts, hooks, scripts, specs, ADRs, SSOT, tests, and generated/reference assets.
- Add or refine rules that govern how agents discover host-specific rules from records.
- Improve Jcode integration files generated by lazy-harness when the change belongs to the framework.

## Forbidden work without explicit confirmation

- Treat a screenshot/transcript shown during harness tuning as a standalone app/UI bug and switch projects without confirming.
- Modify downstream host application behavior as if it were the framework source of truth.
- Push, release, delete worktrees, or perform irreversible host mutations without explicit user confirmation.

## Rule/documentation model

Lazy-harness uses this split:

- `AGENTS.md` = grammar: stable framework-level instructions for when/how agents search, ask, and record.
- `.lazy-harness/{domain,spec,behavior,tests,decisions,ssot}/` = vocabulary: host-specific facts, contracts, decisions, scenarios, and source-of-truth records.
- `.jcode/` = private local harness wiring and personal workflow overlay for this checkout.

Project-specific rules should normally be records, not hardcoded into the shared grammar, unless they are generic enough to apply to every lazy-harness install.

## Implementation map

- Status: `verified`
- Primary files:
  - `.jcode/harness/20-project-rules.md` — private local Jcode note already states this checkout is the standalone lazy-harness source-of-truth repo.
  - `.lazy-harness/AGENTS.md` — shared framework grammar that requires record-first search and user-correction convergence.
  - `.lazy-harness/decisions/0024-ai-first-framework-redesign.md` — defines `AGENTS.md = grammar, record = vocabulary`.
  - `.lazy-harness/decisions/0032-user-correction-ownership-ssot-convergence.md` — requires user corrections about host role/source-of-truth to converge into records.
  - `.lazy-harness/ssot/project-identity.md` — this SSOT record.
- Key symbols:
  - `§2.5 사용자 정정 수렴 규칙` (`.lazy-harness/AGENTS.md`) — operational rule that triggered this record.
  - `AGENTS.md = Grammar` (`.lazy-harness/decisions/0024-ai-first-framework-redesign.md`) — rule/documentation split.
- Flow:
  1. User corrects the agent: current work is harness tuning and this project goal is lazy-harness framework creation.
  2. Agent records that project identity in SSOT before continuing with tuning advice.
  3. Future agents read this record before interpreting ambiguous screenshots/transcripts or project goals.
- Tests / protection:
  - `.lazy-harness/scripts/self-test.py`
  - `python3 .lazy-harness/scripts/doctor.py --profile smoke`
- Ownership boundaries:
  - Owner/upstream: `/home/lazydino/dev/lazy-harness`
  - This host may change: framework grammar, records, hooks, scripts, specs, ADRs, SSOT, tests, generated/reference assets.
  - This host must not change without explicit confirmation: downstream app behavior, releases/pushes, destructive worktree/database operations.
- Cross-layer links:
  - ADR: `.lazy-harness/decisions/0024-ai-first-framework-redesign.md`
  - ADR: `.lazy-harness/decisions/0032-user-correction-ownership-ssot-convergence.md`
  - SDD: `.lazy-harness/spec/platform/implementation-map-standard.md`
- Machine index:
  - graph ids: `kg_ssot_project_identity_lazy_harness_source`, `kg_ssot_project_identity_context_interpretation`
  - generated index key: `pending until implementation-index generator exists`
