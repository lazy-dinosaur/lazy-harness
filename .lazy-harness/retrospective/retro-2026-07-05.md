# Retro report — 2026-07-05

- entries: 7 (open 4 / resolved 3) · levels L1=3 L2=4 L3=0 · skipped-log rows: 1

## Keep (resolved)
- [capture-gate-false-positive] Analysis-discovery-capture STOP gate fired on a turn whose capture was already complete in-record (batch 3 log + queue committed in 3b2afbc before the gate); post-hoc heuristic cannot see that capture happened in-turn → promoted: TDD tests/capture-gate-false-positive.md + backlog item in analysis-discovery-capture-backlog.md (user-approved)
- [capture-gate-false-positive] Second same-session occurrence: gate fired on a pure record-recap turn (next-steps summary read FROM plan/candidates/competitive records; zero new chat-only knowledge) → promoted: same TDD + backlog item
- [capture-gate-false-positive] Third same-session occurrence: gate fired after batch-4 turn whose capture was complete and committed (ebdcb53) before the gate; heuristic does not detect in-turn record writes → promoted: same TDD + backlog item

## Problem (open feedback)
- `fb-mr6a4hus-ou` [L2/premature-execution] Discussion phase: pushed 'what next' execution option gate before requirements formed (ADR 0038 violation, occurrence 1; injection was verified working)
- `fb-mr6a4hvi-ka` [L2/premature-execution] Repeated same-session: second execution-gate push during discussion; corrected by user ('지금 의논중이잖아')
- `fb-mr6a4hw5-xu` [L2/recall-miss-synthesis] TimSquad research missed in first-pass summary: topic scattered across plans/, candidates.jsonl, ADR mentions; user memory corrected
- `fb-mr79avdx-9x` [L2/completion-claim-deployment-gap] Declared update 'complete' while medivance-pwa lacked the Pi package link (skill undeployable there); user pushback exposed it; fixed via lazy pi install --local --package. Completion claims must verify DEPLOYMENT REACH, not just artifact existence

## Try (pattern candidates — REQUIRE user option gate before promotion)
- **capture-gate-false-positive** ×3 (open 0, levels L1)
  - refs: `.lazy-harness/planning/memory-device-implementation-plan.md`, `.lazy-harness/knowledge/candidates.jsonl`

## Vocab harvest (surface-term seeding queue, ADR 0053)
- 팀스쿼드, 하네스 비교, timsquad

> Deterministic aggregation only. Pattern candidates (kind >= 3) require a USER OPTION GATE before promotion to record/policy/capability; vocab terms feed ADR 0053 surface-term seeding via the same gate.
