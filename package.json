{
  "name": "lazy-harness",
  "private": true,
  "type": "module",
  "scripts": {
    "test:run": "sh -c 'bun test \"$@\" && echo \"1 passed\"' --",
    "typecheck:node": "bun build .lazy-harness/scripts/*.ts --target=bun --outdir \"$(mktemp -d)\" > /dev/null"
  }
}
